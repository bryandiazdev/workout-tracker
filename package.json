{
  "name": "workout-tracker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "WorkoutTracker.Client"
  ],
  "scripts": {
    "dev": "cd WorkoutTracker.Client && npm run dev",
    "build": "cd WorkoutTracker.Client && npm run build",
    "start": "cd WorkoutTracker.Client && npm run start"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "mongodb": "^6.5.0"
  }
}
